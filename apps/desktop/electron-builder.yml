appId: com.bubbles.mvp
productName: Bubbles MVP
directories:
  output: dist
  buildResources: build
files:
  - out/**
  - package.json
mac:
  category: public.app-category.productivity
  target:
    - dmg
    - zip
  # Local/internal package only. Developer ID signing, hardened runtime, and notarization are intentionally out of scope for this pass.
  hardenedRuntime: false
  gatekeeperAssess: false
asar: true
